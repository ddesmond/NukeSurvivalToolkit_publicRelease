// Converted from Nuke .nk file: alpha_sum
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "r_plus_g_plus_b" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['clamp==1 ? clamp(r+g+b) : r+g+b']
        },
    ],
}