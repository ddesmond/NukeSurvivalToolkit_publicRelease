// Converted from Nuke .nk file: Lines_Vertical
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Lines_Vertical" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['( sin(x / wide ) + thickness ) / 2', '( sin(x / wide ) + thickness ) / 2', '( sin(x / wide ) + thickness ) / 2', '( sin(x / wide ) + thickness ) / 2']
        },
    ],
}