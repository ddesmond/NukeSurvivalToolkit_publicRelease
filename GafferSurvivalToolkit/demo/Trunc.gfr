// Converted from Nuke .nk file: Trunc
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        GafferImage.Ramp "ColorWheel1" {
            "enabled" : true,
            "gamma" : Imath.Color4f(0.45, 0.45, 0.45, 0.45),
        },
        Gaffer.Expression "Trunc" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['trunc(r*scale)/scale', 'trunc(g*scale)/scale', 'trunc(b*scale)/scale']
        },
    ],
}