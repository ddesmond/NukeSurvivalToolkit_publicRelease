// Converted from Nuke .nk file: Vector_to_UV
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Vector_to_UV" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['(r+x+0.5)/width', '(g+y+0.5)/height', '0']
        },
    ],
}