// Converted from Nuke .nk file: gradient_vertical
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "gradient_vertical" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['y / height', 'y / height', 'y / height', 'y / height']
        },
    ],
}