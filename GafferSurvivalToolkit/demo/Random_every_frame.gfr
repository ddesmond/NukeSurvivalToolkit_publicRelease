// Converted from Nuke .nk file: Random_every_frame
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Random_every_frame" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['random(frame)*random', 'random(frame)*random', 'random(frame)*random', 'random(frame)*random']
        },
    ],
}