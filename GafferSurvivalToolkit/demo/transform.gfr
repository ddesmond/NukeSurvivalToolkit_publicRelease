// Converted from Nuke .nk file: transform
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Transform" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['r(fx,fy)', 'r(fx,fy)', 'r(fx,fy)']
        },
    ],
}