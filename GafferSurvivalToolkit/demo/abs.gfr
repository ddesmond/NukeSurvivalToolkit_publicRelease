// Converted from Nuke .nk file: abs
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        GafferImage.Constant "Constant2" {
            "enabled" : true,
            "color" : Imath.Color4f(1.0, 1.0, 1.0, 1.0),
        },
        GafferImage.Grade "Multiply1" {
            "enabled" : true,
            "channels" : "rgba",
            "multiply" : Imath.Color4f(-1.0, -1.0, -1.0, -1.0),
        },
        Gaffer.Expression "ABS" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['abs(r)', 'abs(g)', 'abs(b)', 'abs(a)']
        },
    ],
}