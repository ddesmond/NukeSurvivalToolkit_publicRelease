// Converted from Nuke .nk file: alpha_binary
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "ifColorThenAlpha" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['(r || b || g) > 0 ? 1 : 0']
        },
    ],
}