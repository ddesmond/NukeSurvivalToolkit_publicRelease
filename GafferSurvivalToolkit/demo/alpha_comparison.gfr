// Converted from Nuke .nk file: alpha_comparison
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "alpha_comparison" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['a<number?r:0', 'a<number?g:0', 'a<number?b:0', 'a<number?a:0']
        },
    ],
}