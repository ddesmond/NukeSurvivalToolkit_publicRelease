// Converted from Nuke .nk file: Lines_Horizontal
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Lines_Horizontal" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['( cos(y / wide ) + thickness ) / 2', '( cos(y / wide ) + thickness ) / 2', '( cos(y / wide ) + thickness ) / 2', '( cos(y / wide ) + thickness ) / 2']
        },
    ],
}