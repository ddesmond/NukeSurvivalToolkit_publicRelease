// Converted from Nuke .nk file: radial
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Radial" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['size - sqrt(x * x + y * y )', '\\nsize - sqrt(x * x + y * y )', '\\nsize - sqrt(x * x + y * y )', '\\nsize - sqrt(x * x + y * y )']
        },
    ],
}