// Converted from Nuke .nk file: Noise
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Noise_expression" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['noise(value)', 'noise(value)', 'noise(value)', 'noise(r,g,b)']
        },
    ],
}