// Converted from Nuke .nk file: Lines_Vertical_Animated
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Lines_Vertical_Animated" {
            "enabled" : true,
            "radius" : 30.0,
            // _note: Nuke Expression with per-pixel math: ['sin((x-width)*((x+width)/(size*1000))+(frame*speed))', 'sin((x-width)*((x+width)/(size*1000))+(frame*speed))', 'sin((x-width)*((x+width)/(size*1000))+(frame*speed))', 'sin((x-width)*((x+width)/(size*1000))+(frame*speed))']
        },
    ],
}