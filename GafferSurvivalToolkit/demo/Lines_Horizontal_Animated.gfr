// Converted from Nuke .nk file: Lines_Horizontal_Animated
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "Lines_Horizontal_Animated" {
            "enabled" : true,
            "radius" : 30.0,
            // _note: Nuke Expression with per-pixel math: ['cos((y-height)*((y+height)/(size*1000))+(frame*speed))', 'cos((y-height)*((y+height)/(size*1000))+(frame*speed))', 'cos((y-height)*((y+height)/(size*1000))+(frame*speed))', 'cos((y-height)*((y+height)/(size*1000))+(frame*speed))']
        },
    ],
}