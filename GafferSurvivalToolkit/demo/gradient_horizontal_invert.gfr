// Converted from Nuke .nk file: gradient_horizontal_invert
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "gradient_horizontal_invert" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['1-(x / width)', '1-(x / width)', '1-(x / width)', '1-(x / width)']
        },
    ],
}