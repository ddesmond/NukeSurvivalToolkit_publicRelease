// Converted from Nuke .nk file: deepToDepth
// Note: Expression-based procedural patterns may need manual conversion
// since Gaffer.Expression works on scalars, not per-pixel data.

Gaffer.ScriptNode {
    children : [
        Gaffer.Expression "DeepToDepth" {
            "enabled" : true,
            // _note: Nuke Expression with per-pixel math: ['deep.r == 0 ? inf : deep.r']
        },
    ],
}